### BLOCKING [evidence]: spx/34-verification.enabler/32-verify.enabler/32-evidence-append.enabler/evidence-append.md:11
Reference: CLAUDE.md states an assertion tagged [test] requires "its co-located test is written via /apply" as the verification mechanism; this enabler spx.status.json already records "overall": "passed", so citing an existing test here asserts the claim is already verified, not pending.
Evidence: The assertion was broadened from "finding evidence validates the finding payload..." to "ALWAYS: scope evidence and finding evidence validate the evidence payload against the selected verification type and evidence kind before appending a journal event ([test](tests/verify-finding.compliance.l1.test.ts))". Reading tests/verify-finding.compliance.l1.test.ts in full: every case calls verifyAppendFindingCommand with review-finding payloads; none call verifyAppendScopeCommand or assert rejection of a scope payload that fails verification-type/evidence-kind validation. tests/verify-payload.compliance.l1.test.ts (the other file touching verifyAppendScopeCommand) only covers payload-required, scope-type, and malformed-changeset checks, not schema validation of a scope payload against the selected verification type. No test in this enabler exercises the new scope-evidence half of the claim.
Required: Either narrow the assertion back to finding evidence only (matching the cited test), or add scope-evidence verification-type/evidence-kind validation test coverage before merging so the recorded passed status matches the widened claim.

### BLOCKING [evidence]: spx/34-verification.enabler/32-verify.enabler/43-terminal-projection.enabler/terminal-projection.md:17
Reference: Same CLAUDE.md [test]-marker rule as above; this enabler spx.status.json also records "overall": "passed".
Evidence: New assertion: "ALWAYS: when a verification type registers terminal validation, finish rejects a supplied terminal status or terminal metadata that conflicts with the run evidence before recording terminal completion or sealing the journal ([test](tests/verify-lifecycle.compliance.l1.test.ts))." Reading tests/verify-lifecycle.compliance.l1.test.ts in full: coverage is blank/invalid terminal status, repeated finish idempotency, seal-write retries, unsealed-run recovery, selector-mismatch rejection, and scope-type/malformed-changeset rejection, none exercise a registered per-verification-type terminal validation rejecting a status or terminal-metadata conflict derived from run evidence. That mechanism is specified for the audit type in 65-audit.enabler/15-audit-payload.pdr.md property 5, whose own spx.status.json is still {} (no recorded verification, per that PLAN.md pending-work list), so no test anywhere in the tree currently backs this claim.
Required: Add a test exercising verification-type-registered terminal validation rejecting a conflicting status/metadata before merging, or defer this assertion out of the already-passed enabler until the audit terminal-validation implementation lands.

